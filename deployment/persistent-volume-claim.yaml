apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-final-pvc
  namespace: mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2
