apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluffy-pet-chart.fullname" . }}
  labels:
    {{- include "fluffy-pet-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluffy-pet-chart.selectorLabels" . | nindent 4 }}
